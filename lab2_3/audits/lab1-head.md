| Code Smell              | Location                   | Confidence | Easy Explanation                                                                                                                                                    |
| ----------------------- | -------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Large Class**         | `include/GameBoard.h:20`   | C4         | `GameBoard` is doing too many jobs. It manages the game board, menus, input, game loop, rendering, and leaderboard.                                                 |
| **Long Method**         | `src/GameBoard.cpp:223`    | C4         | `play_game()` is too long. It handles many things like starting the game, checking the screen size, running the game loop, drawing objects, and ending the game.    |
| **Long Method**         | `src/GameBoard.cpp:361`    | C4         | `step()` is too long. It handles movement, wall collisions, snake collisions, deciding the loser, and eating food all in one function.                              |
| **Duplicate Code**      | `src/GameBoard.cpp:76`     | C4         | `draw_hash_banner_to_win()` and `draw_banner_to_win()` contain almost the same code for drawing the banner.                                                         |
| **Divergent Change**    | `include/GameBoard.h:20`   | C3         | `GameBoard` needs to be changed for many different reasons, such as changing the UI, rendering, 2-player logic, or leaderboard.                                     |
| **Feature Envy**        | `src/GameBoard.cpp:302`    | C3         | `play_game()` directly looks inside `Snake` and `Food` to get their positions and draw them. The `Snake` and `Food` classes could handle their own drawing instead. |
| **Primitive Obsession** | `include/Point.h:10`       | C3         | `Point` only uses two integers (`r` and `c`) to represent a position. It does not provide useful functions for working with positions.                              |
| **Data Class**          | `include/Point.h:10`       | C3         | `Point` only stores row and column values. It does not contain any logic or useful functions.                                                                       |
| **Data Class**          | `include/Leaderboard.h:11` | C3         | `ScoreEntry` only stores a player's name and score. It does not have any functions or behavior.                                                                     |
| **Temporary Field**     | `include/GameBoard.h:66`   | C3         | `play_rows_` and `play_cols_` are only used while the game is running. They are temporary variables and duplicate the existing board size variables.                |
| **Dead Code**           | `include/Snake.h:14`       | C4         | `Snake::init()` is unnecessary because it only calls `reset()`. The extra function does not provide any additional functionality.                                   |
| **Long Method**         | `src/GameBoard.cpp:119`    | C4         | `show_main_menu()` is too long because it handles the menu window, banner, menu options, and user input all in one function.                                        |
| **Long Method**         | `src/GameBoard.cpp:450`    | C4         | `show_game_over_screen()` does too many things. It displays the game-over screen, shows the loser and scores, handles input, and can start the game again.          |
| **Switch Statement**    | `src/Snake.cpp:30`         | C3         | `Snake::set_dir()` has several checks to decide whether the snake can change direction. This makes the function more complicated.                                   |
| **Duplicate Code**      | `src/Leaderboard.cpp:24`   | C3         | `Leaderboard::load()` has similar code for reading player names and scores from the file, which could be combined into one simpler approach.                        |
